import {useEffect,useRef} from 'react';
import type {ObservationPacket} from '../model/types';
export function CameraFrame({packet,label,active=false}:{packet?:ObservationPacket;label:string;active?:boolean}){const canvas=useRef<HTMLCanvasElement>(null);useEffect(()=>{if(!packet||!canvas.current)return;const ctx=canvas.current.getContext('2d');if(!ctx)return;canvas.current.width=packet.calibration.width;canvas.current.height=packet.calibration.height;ctx.putImageData(new ImageData(new Uint8ClampedArray(packet.pixels),packet.calibration.width,packet.calibration.height),0,0);},[packet]);return <div className={`camera-frame ${active?'camera-active':''}`}><div className="camera-title"><span className="signal-dot"/>{label}<span className="mono">{packet?'RGB':'—'}</span></div><div className="camera-image"><canvas ref={canvas}/><div className="camera-reticle"/><span className="camera-time mono">{packet?`${packet.t.toFixed(2)} s`:'ACQUIRING'}</span></div><div className="camera-caption">{packet?`${packet.calibration.width} × ${packet.calibration.height} · simulated optics`:'Preparing camera snapshot'}</div></div>;}

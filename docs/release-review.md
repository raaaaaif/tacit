# TACIT 0.4 release review

Reviewed locally on 25 September 2026. Kernel `tacit-0.4.0`; geometry `nominal-assembly-02`; policy `readiness-policy-2`; study `readiness-study-2`. Publication uses [TACIT on Cloudflare Pages](https://tacit-workbench.pages.dev), connected to [the TACIT repository](https://github.com/raaaaaif/tacit). The delivery report records the exact deployed commit and production checks separately from this local review. The original 0.3 review is preserved in `docs/release-review-0.3.md`.

## Result and evidence

Run, Investigate and Design form a complete evidence workflow: compute a physical episode, inspect original decision inputs and checks, rerun a controlled pair, inspect either arm, and export/import its evidence. The new 96-pair exploratory study contains all 192 assigned arm outcomes. All episodes stop, none reach the target, and none record a modeled violation. Readiness removes 262.17 µL more than fixed + preflight on average (paired 95% interval 211.46–315.76 µL), taking 15.76 s longer. View access improves bulk progress by 57.07 µL; history withholding reduces progress by 64.63 µL. These are synthetic exploratory findings, not physical safety or biological results.

The first study is archived as superseded after a deeper review found an incomplete holder-seating uncertainty allowance. The final revision subtracts the bounded relative seating uncertainty from hardware clearance. A complete new study was run after freezing source hashes. A partial source-review restart is recorded; all final assignments were rerun. Current results use only the final revision. Scene seeds repeat the preliminary study; this is disclosed as exploratory regression evaluation rather than an untouched confirmatory test set. The final canonical report SHA-256 is `9665b48f3b25ab6ec76c1bd52f6c09e4e5fdd29952975bc833c39b1c2f31cb41`.

The 96 pairs reuse 36 unique scene IDs across intervention types. They yield 192 arm outcomes, not 192 independent Bernoulli trials. Recalculation of the frozen rows finds greater removal in 33/36 readiness-versus-preflight pairs and less in 3/36; overhead-view access increases removal in 4/36 pairs and is exactly unchanged in 32/36; withholding history decreases removal in 20/24 pairs and increases it in 4/24. These are descriptive results, not new experiments or an explanation of the counterintuitive cases. Per-comparison denominators and elapsed-time costs accompany the means.

## Scientific snapshot and application release

The 27-file study-2 source inventory is preserved exactly. Release hardening adds an import consistency boundary, recorded-state display handling and worker/UI integration outside those frozen files. The application imports through `src/ui/evidenceImport.ts`; the unchanged lower-level readers alone are not the complete current import contract. No study output is relabelled as a new run, and no source hash is overwritten to conceal a change. Because the evaluation program, scientific inputs and original source snapshot are unchanged, these import and presentation repairs do not require a scientific rerun. A change inside that snapshot would require a newly versioned evaluation.

The final Git commit and source-archive digest identify the complete application, including the new import boundary. The study manifest identifies the historical evaluation program and is not a claim to cover every release UI file. Hashes detect content differences; they do not authenticate an author or independently establish when a source freeze occurred. The report retains its exploratory regression status and repeated seeds.

An external ChatGPT review examined the earlier aa8db6d archive and supplied screenshots. Its findings were treated as advisory counterexamples and design recommendations, then independently checked against the evolving implementation. It did not certify this final code or reproduce our complete dependency-dependent build and browser review. The full downloaded review remains a local working document; the release records incorporated repairs and their own verification evidence.

## Actual browser review

Chrome 154.0.8037.57 was operated against the final production build at localhost. It exercised all three scenarios, fixed and privileged comparators, preflight/readiness policy pairs, view/history interventions, first-blocker navigation, expanded receipts, frame stepping, timeline actions, legacy import, corrupt import, JSON/CSV/case export, paired import, geometry export, search cancellation, a completed 32-candidate nominal search, candidate application and rerun. A separate Playwright WebKit 26.6 engine pass completed Run, receipt inspection, a policy A/B comparison, both-arm export/import, malformed transactional rejection and all three modes at 320/390/768 px, with zero errors, warnings or failed checks. That engine check is not native Safari. A subsequent native Safari 27.0 pass on the public graphite interface ran all three scenario families, verified the 256.3 / 311.4 / 334.3 µL stopped receipts, inspected both policy arms, downloaded and re-imported the paired case, and rejected a malformed decision index while retaining both arms. Safari’s own page menu confirmed 200% zoom; the enlarged receipt and candidate table remained readable. That pass found a modal keyboard issue: Safari could skip dialog controls and focus the background. The repaired dialog explicitly advances Tab/Shift-Tab, contains outside focus and returns focus to the actual opener. Native Safari verified the repaired focus cycle and Escape behavior against the local production build. The delivery report records the subsequent public deployment and verification separately.

The visual revision pairs a graphite specimen field with a light evidence sheet, neutral presentation lighting, stronger typography and explicit unresolved-state colors. One command row precedes the scene; its timeline stays in the specimen column, evidence scrolls with the page, and narrow layouts place the camera toolbar below the canvas. Cutaway and exclusion toggles expose their pressed state. Candidate-specific checks and threshold plots, paired volume balances and zero-referenced intervals use recorded values and retain adverse results. The known stop displays four candidates, with pellet exclusion blocking 4/4 and hardware clearance blocking 1/4. The evidence ledger prominently reports 6/12 independent side intervals covered and 0/4 overhead detections. A CSS filter was removed from the synthetic camera display. Presentation changes leave the scientific optics and all 27 frozen source hashes unchanged. The research and rationale are in [the visual design review](visual-design-review.md).

Responsive inspection covered all six widths of 320, 390, 768, 1024, 1440 and 1920 CSS px without page overflow; at 390 × 844 the primary command ended at 427 px and was visible without scrolling. Final expanded-receipt checks at 320, 390 and 768 px and CSS 200% zoom passed; the layout stacks at the available content width. A zoom-induced canvas sizing problem was corrected using layout pixels. The mobile truth label remains visible. The final computed-style contrast scan found zero failures across all three modes within its scope, excluding disabled controls and gradients. This is not a formal WCAG conformance assessment.

Keyboard operation completed Run, receipt investigation and paired comparison. The About dialog traps keyboard focus, Escape closes it and focus returns to the originating button. Reduced-motion preferences leave computed replay paused at zero. Global shortcuts do not fire inside form controls or the modal; final evidence-table keyboard checks left replay time unchanged. Replay/Space is separate from the Run simulation action. An instrumented cancel/restart check recorded one active worker and a peak of one; worker identity and job IDs prevent stale completion. A final lifecycle review found replay continuing while Design's geometry was collapsed; a targeted visibility effect now pauses it. The repaired run/comparison cancellation, resize and historical-expansion flows passed without browser errors. The revised interface was also checked in an isolated browser with WebGL deliberately disabled: computation, receipt inspection and export controls remained available. A dedicated VoiceOver/screen-reader pass was not performed; these checks do not establish assistive-technology coverage or formal accessibility conformance.

A malformed case was rejected without changing the visible decision, replay time or the entire exported packet. Additional malformed event-index, terminal-outcome and decision-check imports were rejected transactionally before changing the current case. Import consistency checks also reject a policy comparison containing identical controllers under different arm labels, even when its hashes have been recomputed. Paired export/import preserved both scientific arms, including every event, pixel, source reference and manifest. Re-export marks transport provenance as recorded and therefore produces a new outer hash; it is not claimed to be byte-identical to the live packet. A legacy fixture with a recorded violation followed by a stop still displays the violation as the outcome. Legacy traces never acquire invented receipts or substitute current geometry as an exact replay.

Automated tests recreate all 13 textual external-review counterexamples at the actual application import boundary: 11 trace/receipt mutations and two misleading pair contracts, including recomputed valid hashes. Additional cases cover missing/duplicate packets, missing required checks/manifests, incompatible manifests, deeply nested action data, truncated JSON and obsolete import successes/errors. Genuine current controller, legacy and paired files remain positive controls. This verifies `readImportedEvidence`; it is not a claim that the external audit's direct calls to the preserved low-level readers now pass its repair assertion.

Rerunning an inspected arm now preserves its history and view intervention. The checked history-withheld rerun produced the same result and event sequence before and after rerunning, and its initial display retains the original unresolved belief until recorded evidence becomes available. Switching from a missing-context population filter to the Context intervention recovers the applicable population instead of hiding the selector behind an empty state.

A browser test deliberately delayed the first known-case read, requested a shifted case next, and verified that the obsolete read could not replace the selected case after completing. Changing the prepared scenario cleared the previous illustrative comparison. Recorded volume readouts use saved initial mass and playback quantities, and comparison settings explicitly identify the default policies actually run. Numeric checks cover all population/family subsets, elapsed-time cost, interval sign conversion, retained zero-effect pairs and per-comparison denominators.

Repairs discovered through computer use included stale background playback, zero-sized hidden WebGL render targets, paired imports losing their second arm, hidden mobile truth labels, a timeline range input intercepting action-button clicks, zoomed canvas sizing, and ambiguous labels on unchecked/privileged comparators. The actual interface was retested after each repair.

## Animation and performance

A continuous recording covers complete known, changed and missing-context episodes at 2× playback. Ninety-nine screenshots cover immediately before, at and after all 33 timed action starts; all three contact sheets were visually inspected. The continuous recording was inspected at distributed frames as well. This is a boundary-focused visual review, not a claim that every rendered frame was manually watched. Independent playback checks sampled all 6,697 frames at 60 Hz across those three episodes and conserved tube-plus-tip volume. Existing regressions also check speed limits, settling, capture-time visibility and action-boundary continuity.

The final continuous local pass through known, changed and missing-context scenarios recorded computation times of 2.869, 3.376 and 2.840 s, respectively. Render telemetry counted 3,300 active frames across the pass. Its end-of-scenario rolling snapshots reported medians of 16.6, 16.7 and 16.7 ms and p95 values of 19.1, 18.6 and 18.1 ms. Each snapshot uses the latest 600 retained intervals and excludes gaps of 150 ms or longer; these are not percentiles over every frame in an episode. The pass recorded 117–119 draw calls, 61–67 geometries across visited variants, and zero console warnings/errors. The recording and sampled contact sheet were inspected. These are local measurements on the test Mac, not universal device guarantees.

## Numerical, inference and artifact checks

- All 45 automated tests passed in the final local run (28.545 s; zero failures), covering physical accounting, geometry, uncertainty and timing invariants, receipts, import schemas and consistency, asynchronous import order, deterministic pairing, old benchmark accounting and playback.
- Nominal assembly tests cover 32 supported tilt/index/tolerance combinations, explicit support contact, whole outer tube intersection at sampled insertion positions, cap/hinge surface samples, and CAD/analytic/optical alignment. These are sampled computational checks, not continuous insertion proofs or measured fit.
- Independently parsed 0° and 5° downloads have 1,104 and 1,176 triangles. Every edge is shared by exactly two triangles; STL and 3MF coordinates match exactly in mm. The 10° export is disabled and execution stops outside the assembly contract.
- Independent SciPy quadrature/root finding on 96 cases found maximum height disagreement 0.000521 mm and volume disagreement 0.02991 µL.
- Internal synthetic volume interval coverage is 36/36; pump and calibration sensitivity cover nine development cases. This does not establish real-camera calibration.
- The new independent 16-image Cycles grid detects every side liquid boundary but covers only 6/12 nominal feature intervals, with maximum level error 1.6393 mm, and misses all four overhead pellet features. Original five-image failures and the nonconvergent 10° surface case remain preserved.

Formatting, TypeScript and the production build pass. The recorded final build produced 7,878,378 raw bytes and a conservative 1,911,123-byte compressed whole-site sum, within the existing 12 MB / 2.5 MB budgets. That sum includes optional evidence, CAD code and all holder variants; it is not measured initial network transfer. Exact deployed bytes and archive hashes are recorded in the delivery report. Assets, fonts and computation are local; no telemetry, external model service or account is needed. All 27 frozen scientific source hashes still match, and CI checks that snapshot before running the test suite.

## Delivery boundary

Source, frozen study, independent images, methods, acceptance map, application-response draft and demonstration script are included. Publication uses the existing Git-connected Cloudflare Pages project. The delivery report records the release commit and observed public checks of live assets, current evidence and the end-to-end browser flow. The holder is research geometry and is explicitly not for fabrication. No physical experiment, biological validation or application submission was performed.

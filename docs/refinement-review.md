# TACIT refinement audit and acceptance map

The supplied handoffs and standalone reviewer were reviewed as design evidence. The reference kit was not substituted for the original application. Changes are implemented in the original React/TypeScript kernel, worker and interface. The original handoff files remain preserved privately. The 0.4 release uses the existing repository and Cloudflare Pages site; publication checks are recorded in the delivery report. No application response was submitted.

## Geometry and physical contract

A01–A05 / G01–G07: repaired the index bore and bottom support, declared the assembly datum and unrestrained degrees of freedom, regenerated all holder assets, and aligned collision and optical masks. Regression tests cover the original interference witness, 32 supported tilt/index/tolerance cases, sampled whole-body insertion and Manifold/analytic boundary agreement. The downloaded 0° holder has 1,104 triangles and 1,656 closed two-manifold edges; STL and 3MF agree exactly in mm. The visual marker is not a mechanical key. Physical fit and fabrication remain unvalidated.

P01–P04 / A11, A15, A16: volume conservation, tip displacement, full-stroke immersion, bounded motion and capacity are tested. The failed 10° equilibrium case remains excluded. No new fluid-dynamics or biological claim was introduced. Candidate checks now include the bounded separation between the estimated tube pose and stationary holder pose. This conservative allowance is included in the final policy revision 2 and its newly frozen study.

## Evidence and inference

A10, A13, A14, A20 / O01–O04, O06, D01–D04: missing history preserves an unresolved angular envelope while permitting distant bulk progress when checked. The existing persistent calibration nuisance and duplicate-exposure protection were retained. Ordinary planning inputs are copied by whitelist; evaluator-only fields cannot affect them. Receipts are committed at decision time and cannot contain later observations. Required checks cannot be absent or unknown on an action labelled as passing. Bounded search never claims universal infeasibility. Legacy rationale remains explicitly absent.

A12 / O05: preserved the original independent five-image failures and added a frozen 16-image independent Cycles nuisance grid. The unchanged extractor detects 12/12 side levels but nominal feature intervals cover only 6/12; all four overhead pellet detections fail. Self-rendered 36/36 volume coverage is labelled an internal diagnostic and does not cancel these failures. Pump-error and camera-bias sensitivity use development scenes only.

## Evaluation and provenance

A06–A09, A17–A19 / D05, E01–E08: retained original aggregates under 0.3, including 117 completions, 390 stops and the 18-stop tuned oracle negative result. Historical CSVs provide final outcomes, not complete action logs, so historical attempted-aspiration exposure remains unavailable rather than inferred. Original optimization trades residual/time penalties under a finite candidate search; its oracle is a privileged comparator, not an optimum or safety guarantee. No claim that two versus one violations establishes improvement is retained.

New paired reruns record exact initial physical identity, intervention access, all assigned outcomes, aspiration exposure and stop stage. History masks information without changing the world. View access and policy comparisons rerun from initialization. Bootstrap keeps pairs and scenario-family weights intact. Zero-event bounds and degenerate completion intervals remain visible. Assignment seeds exclude the development examples, but repeat the superseded preliminary study after the code-driven repair; that reuse and prior historical reuse are disclosed. The current study is exploratory, not confirmatory. Source hashes were frozen before the final complete rerun.

The first new study is archived as superseded after deeper review found the holder-seating uncertainty gap. A partial second attempt was stopped during source review; the complete final assignment list was restarted. Neither intermediate result is substituted for the final policy revision. This is an administrative restart, not an omitted adverse episode.

## Interface and artifacts

A21–A24 / U01–U07, X01–X03: camera housings and supports remain explicitly outside modeled collision geometry. Run leads to a real receipt, then a matched comparison and a portable packet. Imports validate the complete packet before committing; corrupt data leaves the previous case intact. SHA-256 is local integrity, not external authentication. Paired imports restore both arms. Current and historical scientific views are separated.

Actual Chrome and native Safari interaction verifies computation, pause/replay, receipts, paired comparison, export and import. Responsive layouts, keyboard focus, reduced motion, cancellation, stale worker protection, missing WebGL, animation boundaries and performance are reviewed in the release report. Tests of the reference HTML alone are not counted as original-app tests.

A25: physical measurements were optional in the supplied refinement. None were performed or implied. A future measured claim requires an independently defined protocol and raw data; that is outside this computational release.
